{
  "name": "vault-sync-lite",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "bun run esbuild.config.mjs --watch",
    "build": "bun run esbuild.config.mjs --production"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "esbuild": "^0.23.0",
    "obsidian": "^1.5.7",
    "typescript": "^5.5.0"
  }
}
